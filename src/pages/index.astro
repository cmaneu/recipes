---
import { DEFAULT_LOCALE } from '../i18n';

// The language is always present in the path, so the site root redirects to the
// default locale (English).
const base = import.meta.env.BASE_URL.replace(/\/$/, '');
return Astro.redirect(`${base}/${DEFAULT_LOCALE}/`);
---
